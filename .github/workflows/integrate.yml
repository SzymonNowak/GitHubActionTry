name: Node Continuoues Intergration
on:
  push:
    branches: [master]

jobs:
  test_push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Test
      - run: npm ci
      - run: npm test
      - run: npm run e2e
      - run: npm run build
